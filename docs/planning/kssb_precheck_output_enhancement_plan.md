# KSSB 공시근거 사전검토 산출물 품질 고도화 방안 (범용·문서 비특정)

> **성격**: docs-only 설계 문서. **이번 K-water 사례에 국한하지 않고 모든 입력 지속가능성 보고서에 일반 적용** 가능한 산출물
> 고도화 방안이다. **판정 로직·스키마·validator·delivery·manifest 무변경**을 전제로 하며, 각 규칙의 실제 구현은
> **별도 remediation 사이클(Codex review 게이트)**로 진행한다. submission 준비 완료·제품 완성·2N-5·OCR·provider 확정 주장 없음.
>
> **근거 3종**:
> 1. **Claude 실사용 검토** — `docs/planning/usertest_output_quality_review_2026-07-09.md` (형식·인용축 Q1~Q5 / 내용축 C1~C5 / KSSB 확정기준 대조 A1~A7). 인용 실재성 PyMuPDF 대조(18건 중 17건 실재).
> 2. **ChatGPT 분석 보고서** — `kwater_kssb_precheck_improvement_report.md` (구조·컨설팅 가치축 및 **공시 준비 과제별 질문 세트** 기여). *비고: 해당 파일은 UTF-8 콘텐츠가 모지바케로 표시되는 인코딩 이슈가 있었으나(→ B3-MIN-02 인코딩 findings의 메타 사례), 내용은 해독해 반영. 본 문서는 clean UTF-8로 작성.*
> 3. **삼일 PwC KSSB 로드맵/확정기준 자료** (2026.03, S/N 2603W-RP-035) 및 [[kssb-standard-2026-finalized]].

## 0. 방향 재정의 (범용)

- **현 산출물의 성격**: "KSSB 4대 영역 **항목별 근거 매칭 보고서**"(source-bound 근거 확인은 견고 — 실사용 인용 실재성 17/18 실증).
- **목표 성격**: "**KSSB 공시 준비도 사전진단 초안**" — 근거 매칭을 넘어, 발행사가 **의무 공시 시점에 무엇을 준비해야 하는지**를 함께 보여주는 컨설턴트 검수용 초안.
- **4개 개선 축** (모든 문서 공통):
  - **Q — 인용 무결성**(형식): 인용이 신뢰 가능한 연속 원문 발췌인가.
  - **C — 내용·판정 정합**: 라벨이 근거에 부합하고, 수치·해석이 정합하는가.
  - **A — KSSB 확정기준 정합**: 확정 공시기준 용어·경과규정·선택공시 구조에 맞는가.
  - **G — 구조·컨설팅 가치**(ChatGPT 기여 통합): 준비도 진단·준비과제 gap·교차 질문 세트로 컨설팅 실효성이 있는가.
- **불변 경계(4축 공통)**: source-bound(인용 임의 변경 금지) · detect-only(경고는 판정 직접 매핑 금지) · **재판정 금지**(렌더러/후처리) · no-overclaim · 사람 검수 경계 · 미공시 단정 금지.

## 1. 두 리뷰 종합 — 수렴·차이·채택 판단

| 주제 | Claude 검토 | ChatGPT 3.x | 판단 |
|---|---|---|---|
| 내부탄소가격 | **A1**: KSSB 선택공시(optional)로 근거 정밀화 | 3.1: "적용대상 아님" → "선택공시·운영여부 확인 필요" | **수렴·채택**(AR1). 라벨 자체 변경은 재판정 아닌 **프레이밍/근거** 조정으로 한정 |
| Scope 3 | **A2**: 경과규정 3년 유예(2031) 맥락(C2 refine) | 3.8: "미충족" 아닌 "선제 구축 필요" | **수렴·채택**(AR2). 유예 + 선제구축 결합 |
| 재무영향 | **C3/A3**: 재무 시나리오≠기후 시나리오 + Impact Pathway | 3.5: Impact Pathway 중심 강화 | **수렴·채택**(AR3/CR2) |
| 목표 수치 | **C1**: 목표 배출 *수준* vs 감축 *량* 혼용(p.100 교차검증) | 3.10: 목표 경로 vs 실제 진척 분리 | **보완적·채택**(CR1). 두 관점(수준/량 + 경로/실적) 모두 반영 |
| 거버넌스 R&R | **A4/C4**: 작성·승인·총괄 R&R + 발행 승인일·승인자 | 3.2/3.3: gov-01/02 R&R·승인체계 보강 | **수렴·채택**(AR4) |
| 인용 무결성 | **Q1/Q2**: 비연속 표 인용·열매핑 부재(고유 발견) | (미언급) | **Claude 고유·유지**(QR1~QR4) |
| 내부 상태 문자열 | **Q3**: =True·영문키 비노출 | (미언급) | **Claude 고유·유지**(UR1) |
| 구조 확장 | (부분) | 3.4·4·5: 준비도 섹션·5대 과제 gap | **ChatGPT 고유·채택(범위 구분)**(G축) |
| 교차 질문 | (부분) | 6.x: Scoping·ICSR·Impact Pathway 질문 | **ChatGPT 고유·채택(CORE/EXTENDED 구분)**(GR2/GR5) |
| 위험관리 범위 | (C4만) | 3.6: "지속가능성 위험 전반" 과대포괄 → 기후·사회 중심 명확화 | **ChatGPT 고유·채택**(CR2 계열) |

- **범위 주의(EXTENDED로 강등)**: ChatGPT의 **전면 ICSR 질문 battery·연결기준 Scoping 심화 섹션**은 대부분 **공개자료로 검증 불가한 "고객 질의" 성격**이다. 유익하나, 이를 무분별 확대하면 *공개자료 근거 사전검토*가 *준비도 컨설팅 진단*으로 성격이 바뀌어 **과대약속** 위험이 있다 → **EXTENDED 계층으로 명확히 경계**(§2-G).
- **Claude C2 refine 확정**: "Scope3 일부 근거 확인 관대" 관찰은 **A2 유예 맥락 + ChatGPT '선제 구축'** 으로 대체 — 라벨 유지, 근거설명에 유예·선제구축 명시.

## 2. 범용 개선 규칙 (문서 비특정)

> 규칙은 특정 항목ID가 아니라 **공시 영역/패턴** 기준으로 기술한다. K-water는 예시로만 인용한다.

### 2-Q. 인용 무결성 규칙
- **QR1 — 비연속 인용 금지**: `evidence_anchors[].quote`는 **단일 연속 원문 스팬**이어야 한다. 표/색인에서 여러 셀을 가로질러 이어붙이지 않는다.
  표 파생 근거는 셀 값 하나만 인용하고, 분류번호·페이지·마커 등은 **인용 밖(위치·근거설명)**에 서술하거나 "표 셀 요약(비연속)"으로 명시.
  *(예: `온실가스 E-2-2 온실가스 배출량(Scope3) - ○`처럼 헤더+코드+마커를 가로질러 붙인 문자열은 verbatim 감사에서 실패.)*
- **QR2 — 수치 표 인용의 매핑 명시**: 연도 헤더 없는 숫자 나열을 인용할 때 근거설명에 **열↔기간(연도)·단위 매핑**을 명시.
- **QR3 — validator detect-only 경고 후보(R3/R4)**: `quote`가 표 파생으로 보이면(마커 글리프 `●◐○` 포함 / 코드토큰(`X-N-N`)+빈셀(`-`) 혼재 / 다열 숫자행) → *"표에서 재구성된 인용일 수 있습니다; 연속 원문·열 정렬을 사람이 확인하세요."* **warning-only·additive·Node 한정·기존 parity fixture 미발화**(Phase 3-B R1/R2 패턴 계승).
- **QR4 — 추출 아티팩트 불교정**: 아래첨자 분리(`tCO₂`→`tCO 2`) 등은 실행 추출값 그대로 유지(렌더러 임의 교정 금지, source-bound). 한계 고지로 커버.

### 2-C. 내용·판정 정합 규칙
- **CR1 — 수치 의미 정합**: 동일 수치가 서로 다른 의미(예: 목표 **배출 수준** vs 누적 **감축량**, 총량 vs 원단위)로 병치되면 조정하거나 근거설명에서 구분한다. **가능하면 다른 페이지 수치로 교차검증**(예: 목표치 vs 실적치의 정합성).
- **CR2 — 근거-요구사항 관련성**: 인용 맥락이 공시요구와 다른 축이면(예: **재무리스크 시나리오 ≠ 기후 시나리오**, 전사 위험관리 ≠ 지속가능성 특정 위험) 근거설명에서 그 구분을 명시. 항목명이 과대포괄로 읽히면 실제 근거 범위로 좁혀 서술.
- **CR3 — 라벨 보정 신호**: 원문의 **자기 색인/자체 평가가 "미충족·미공시"로 명시**한 항목은 근거설명에 그 사실을 명시한다. *라벨 재판정은 하지 않되*(executor 경계), 검수자가 라벨을 보정할 수 있는 신호를 남긴다.
- **CR4 — 근거 선택 강도**: 조각·다이어그램 라벨보다 **온전한 문장**을, 정의문보다 **실제 활동·수치 서술**을 우선 인용.

### 2-A. KSSB 확정기준 정합 규칙 (모든 발행사 공통)
- **AR1 — 내부탄소가격 = 선택공시(optional)**: 확정 기준상 "톤당 내부탄소가격"은 선택 공시 항목이다. 미확인 시 "검토 범위 외/적용대상 아님"으로 단정하지 말고 **"KSSB 선택공시 항목 · 운영 여부 확인 필요"**로 프레이밍.
- **AR2 — Scope 3 = 경과규정 3년 유예(2031)**: 정량 부재는 최초공시(FY2027) 기준 **유예 범위 내**로 즉시 결함이 아니다. **"유예 기간 내 · 데이터 체계 선제 구축 권고"**로 표현.
- **AR3 — 재무영향 = Impact Pathway**: 기후 위험·기회가 **비용·투자·자산·수익·현금흐름**으로 이어지는 **영향 경로**를 **계정과목·예산·투자 데이터와 연결**하도록 질문·권고를 구성(정성 서술만으로 불충분).
- **AR4 — 거버넌스 = R&R + 승인 provenance**: **작성자·승인자·총괄책임자 R&R**과 **지속가능성 재무정보 발행 승인일·승인자 공시**를 질문에 포함. 이사회·경영진·재무·지속가능성 조직 간 연결 구조를 묻는다.
- **AR5 — 생략 시 사실·근거 기술(설계 근거)**: 확정 기준의 "중요정보 없어 생략 시 그 사실·근거 기술" 요구가 플러그인의 **미공시 단정 금지 + 부족정보/사유 기술 + 고객질문 연결** 설계를 뒷받침 → **유지**(변경 불요), 고지 문구 근거로 인용 가능.
- **AR6 — 기간 정의 표준화**: **단기(1년)·중기(1~5년)·장기(5년)**(산업 특성 시 변경 가능)에 맞춰 전략/위험 기간 구분 권고를 표준화.
- **AR7 — 온실가스 방법별 구분공시**: 배출량 근거·권고에 **산정 방법론·경계·배출계수·방법별 구분**을 포함.
- **AR8 — 시점·적용대상 컨텍스트**: 시행 FY2027(2028)·연결자산 30조원↑ 단계적 적용. 산출물이 **대상사 규모·보고연도를 확정하지 못하면 "현재 미충족이 곧 위반은 아님"** 컨텍스트를 붙이고 단정을 회피.

### 2-G. 구조·컨설팅 가치 규칙 (ChatGPT 기여 통합 — CORE / EXTENDED 계층)
**CORE (MVP 내 안전 — source-bound·미공시 단정 금지 유지):**
- **GR1 — 제도 적용성·준비도 판단 섹션(짧게)**: 산출물에 "제도 적용 시점·준비도" 짧은 판단을 추가. 대상사 규모·연도 미상 시 단정 회피 문구(AR8) 필수.
- **GR2 — 고객질문 이중 분류**: 질문을 **KSSB 4영역** + **공시 준비 축(거버넌스 R&R·Scoping·기후데이터·Impact Pathway·내부통제)**으로 이중 태깅.
- **GR3 — 준비축 질문의 provenance 규율**: 준비축 질문을 missing_info→customer_question 경로에 **additive**로 추가하되, 근거는 **"공개자료에서 확인되지 않음"** provenance만 사용(없는 근거를 발견한 것처럼 날조 금지).

**EXTENDED (선택·명확 경계 — 별도 사이클):**
- **GR4 — 준비 5대 과제 gap 요약표**: (준비과제 × 현재 공개자료 확인 × 주요 공백 × 요청자료). 컨설팅 실효성↑.
- **GR5 — 연결기준 Scoping · ICSR 심화 질문**: 공개자료 검증 밖 **"고객 질의" 성격 명시**. 판정·근거 확인 주장 금지, 준비도 컨텍스트로만.
- **경계**: G축(특히 GR1/GR4/GR5)은 report_template·SKILL 콘텐츠 변경 → **각각 별도 remediation + Codex review**. 스키마·판정 로직 무변경, 재판정 금지.

### 2-U. UX·표현 규칙
- **UR1 — 내부 상태 문자열 비노출**: `수행=True`·영문 키(`prohibited_terms_check`) 등 기계 상태를 **자연어 산문**으로. (렌더러 3포맷 + Python + parity fixture 동시 수정 필요.)
- **UR2 — 워크어라운드 나레이션 비노출**: 인코딩·재읽기·파일 처리 내부 동작을 사용자 출력에 노출하지 않음(기존 B5 §2, BOM 금지). 산출물·안내 파일은 UTF-8 간주.
- **UR3 — 경로·계정 비노출**: 로컬 절대경로·계정명 비노출(에이전트 나레이션 포함).

## 3. 카탈로그 영역별 범용 가이드 (항목 비특정)

| 영역 | KSSB 핵심 공시요구 | 흔한 공백 패턴 | 정합 질문·권고 템플릿(요지) |
|---|---|---|---|
| 거버넌스 | 위험·기회 감독 방법 | 조직 구조는 있으나 감독 활동/주기/보고경로/R&R 부재 | R&R(작성·승인·총괄), 감독 안건·주기, 발행 승인일·승인자 (AR4) |
| 전략 | 위험·기회의 영향·대응·회복력 | 위험은 있으나 기회·기간구분·재무영향 경로 부재 | 위험/기회 분리, 단·중·장기(AR6), Impact Pathway(AR3) |
| 위험관리 | 위험 식별·평가·관리 프로세스 | 기후·사회 위험만 있고 "지속가능성 전반"으로 과대포괄 | 실제 근거 범위로 축소(CR2), 평가주기·통합·책임부서 |
| 지표·목표 | 지표(단위·기간)·목표·진척 | 표 인용 열매핑 부재(QR2), Scope3 유예(AR2), 목표 수준 vs 감축량(CR1), 방법별 구분(AR7), 내부탄소가격 선택공시(AR1) | 열↔기간 매핑, 유예·선제구축, 기준연도·경계·실적, 산정 방법론 |

## 4. 실행 라우팅 (레이어별 — 각 = 게이트 사이클)

| 규칙군 | 실행 레이어 | 위험도 | 스키마·판정로직 |
|---|---|---|---|
| QR1·QR2·CR1~CR4·AR1~AR8·GR2·GR3 | **상류 findings 생성**(SKILL.md 인용/근거설명/질문/권고 프롬프트 지침) | 중 | 불가침 |
| QR3 | **validator detect-only**(R3/R4, Node additive, parity 미발화) | 저 | 판정 로직 무변경 |
| UR1 | **렌더러 3포맷 + Python + parity fixture** | 중 | 스키마 무변경 |
| GR1·GR4·GR5 | **report_template·SKILL 구조 확장** | 중~고 | 스키마·판정 무변경 |
| UR2·UR3 | **SKILL.md 지침**(기존 B5 §2 정합) | 저 | — |
| AR5 | 설계 확인(변경 불요) | — | — |

## 5. 우선순위 (범용)

- **P1 (라벨·정확도 직접 영향)**: AR1(내부탄소가격 선택공시) · AR2(Scope3 유예) · CR1(수치 의미) · QR1(비연속 인용).
- **P2 (표준 정합·구조)**: AR3/AR4(Impact Pathway·R&R 질문) · GR1/GR2(준비도 섹션·이중분류) · UR1(상태 문자열).
- **P3 (자동 감지·심화)**: QR3(validator detect-only) · GR4/GR5(EXTENDED) · CR3/CR4(라벨 신호·근거 강도) · AR6/AR7.

## 6. 경계 / 성격

- **docs-only 설계**. 판정 로직·스키마·validator·delivery·manifest **무변경**. detect-only·source-bound·**재판정 금지**·no-overclaim·미공시 단정 금지·사람 검수 경계 유지.
- **executor 경계**: 본 문서는 라벨을 확정·대체하지 않는다(PASS/FAIL 아님). CR3 등 라벨 관련 항목은 **검수 신호**이지 재판정이 아니다.
- **범위 정직성**: MVP가 KSSB 전체 기준을 커버한다는 주장 아님. G축 EXTENDED는 **준비도 컨텍스트**로 경계하며 공개자료 검증을 넘어서는 부분은 "고객 질의" 성격을 명시.
- **KSSB 시점**: 확정 기준 시행은 FY2027(2028)이므로, 이전 발행 보고서의 현재 미충족이 곧 위반은 아님(AR8).
- **각 규칙 실행 = 별도 remediation 사이클(Codex review 게이트)**. **다음: 본 고도화 방안에 대한 Codex review 대기** → 승인 후 P1부터 편성(사용자/ChatGPT 지시).
